import { EB_Garamond, Fragment_Mono } from 'next/font/google'

const fragment = Fragment_Mono({ subsets: ['latin'], weight: '400' })
const garamond = EB_Garamond({ subsets: ['latin'] })

export { fragment, garamond }
